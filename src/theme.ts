import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#059669",
      light: "#fef2f2",
      dark: "#000000",
      contrastText: "#ffffff",
    },
  },
});

export default theme;
